export enum OrderStatus {
  PENDING_CONFIRMATION = 'pending_confirmation',
  PROCESSING = 'processing',
  SHIPPING = 'shipping',
  SUCCESS = 'success',
  CANCELLED = 'cancelled',
  RETURNED = 'returned',
}
